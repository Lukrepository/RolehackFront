<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="com.tbd.forkfront">

	<uses-sdk/>

	<!-- Rolehack: no permissions.  The network was only for Hearse, which
	     Rolehack switches off, and the game's files live in the app's own
	     storage, which needs none from Android 10 on (the app needs 11). -->

	<application/>
</manifest>
